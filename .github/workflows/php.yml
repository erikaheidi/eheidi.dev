name: PHP Composer

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Validate composer.json and composer.lock
      run: ./blog-en/composer validate

    - name: Install dependencies
      run: ./blog-en/composer install --prefer-dist --no-progress

    - name: Check code style
      run: ./blog-en/composer test:lint

    - name: Run test suite
      run: ./blog-en/composer test:unit
